export default function Navbar(){
    return(
        <header>
            <nav>
                <a href="#hero" className="text-3xl font-bold text-black">
                    Home
                </a>

            </nav>
        </header>
    );
}